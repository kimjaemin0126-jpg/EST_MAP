# Local dependencies and build caches
node_modules/
dist/
.vercel/
*.log

# Local environment files
.env
.env.local

# Large source data is kept locally; generated browser data is committed.
src/data/서울시 일반음식점 인허가 정보.csv
src/data/서울시 휴게음식점 인허가 정보.csv
src/data/서울시 미용업 인허가 정보.csv
src/data/~$*

# Canonical local source-data workspace. Keep only the preprocessing script in Git.
node_modules/
DP/data/
DP/clean_data/
*.shp
# 대용량 생성 데이터
**/rows_parsed.json
**/generated/

# Reproducible source datasets and GIS source files are kept locally.
src/data/*.csv
src/data/*.shp
src/data/*.shx
src/data/*.dbf
src/data/*.prj
src/data/*.cpg

# Python bytecode is never part of a deployable source snapshot.
__pycache__/
*.py[cod]

# Nested copies of this repository are not part of the active application.
EST_MAP/
